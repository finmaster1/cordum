version: "3.9"

services:
  nats:
    image: nats:2.10-alpine
    ports:
      - "4222:4222"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_KEEP_ALIVE=24h
    volumes:
      - ollama_data:/root/.ollama

  coretex-safety-kernel:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-safety-kernel
    depends_on:
      - nats
    environment:
      - NATS_URL=nats://nats:4222
      - SAFETY_POLICY_PATH=/etc/coretex/safety.yaml
    volumes:
      - ./config/safety.yaml:/etc/coretex/safety.yaml:ro

  coretex-scheduler:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-scheduler
    depends_on:
      - nats
      - redis
      - coretex-safety-kernel
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379
      - SAFETY_KERNEL_ADDR=coretex-safety-kernel:50051
      - POOL_CONFIG_PATH=/etc/coretex/pools.yaml
      - TIMEOUT_CONFIG_PATH=/etc/coretex/timeouts.yaml
    volumes:
      - ./config/pools.yaml:/etc/coretex/pools.yaml:ro
      - ./config/timeouts.yaml:/etc/coretex/timeouts.yaml:ro

  coretex-api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-api-gateway
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379
      - SAFETY_KERNEL_ADDR=coretex-safety-kernel:50051
      - API_KEY=[REDACTED]
      - TENANT_ID=default
    ports:
      - "8080:8080"
      - "8081:8081"
      - "9092:9092"

  coretex-worker-echo:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-echo
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-chat:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-chat
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-chat-advanced:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-chat-advanced
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=llama3

  coretex-worker-code-llm:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-code-llm
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=llama3

  coretex-worker-orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-orchestrator
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379
      - TIMEOUT_CONFIG_PATH=/etc/coretex/timeouts.yaml
      - USE_PLANNER=true
      - PLANNER_TOPIC=job.workflow.plan
    volumes:
      - ./config/timeouts.yaml:/etc/coretex/timeouts.yaml:ro

  coretex-worker-planner:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-planner
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-scan:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-scan
    depends_on:
      - nats
      - redis
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-sast:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-sast
    depends_on:
      - nats
      - redis
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-partition:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-partition
    depends_on:
      - nats
      - redis
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-lint:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-lint
    depends_on:
      - nats
      - redis
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-tests:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-tests
    depends_on:
      - nats
      - redis
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-report:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-report
    depends_on:
      - nats
      - redis
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379

  coretex-worker-repo-orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SERVICE: coretex-worker-repo-orchestrator
    depends_on:
      - nats
      - redis
      - coretex-scheduler
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_URL=redis://redis:6379
      - TIMEOUT_CONFIG_PATH=/etc/coretex/timeouts.yaml
      - REPO_REVIEW_MAX_FILES=500
      - REPO_REVIEW_BATCH_SIZE=50
      - REPO_REVIEW_MAX_BATCHES=5
      - REPO_REVIEW_RUN_TESTS=false
    volumes:
      - ./config/timeouts.yaml:/etc/coretex/timeouts.yaml:ro

volumes:
  ollama_data:
