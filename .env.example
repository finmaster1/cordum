# Gateway auth (sent as X-API-Key)
# Required: set a strong API key before running.
CORDUM_API_KEY=
CORDUM_SUPER_SECRET_API_TOKEN=
# Default tenant for single-tenant mode (HTTP clients send X-Tenant-ID)
TENANT_ID=default
