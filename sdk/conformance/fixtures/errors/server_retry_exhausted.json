{
  "schemaVersion": 1,
  "name": "errors.server_retry_exhausted",
  "description": "Simulator returns 500 for the first three requests to this operationId; SDK's retry policy (max 3) gives up on the third and raises RetryExhaustedError. The last response must be preserved on the exception.",
  "tags": ["errors", "retry", "server"],
  "setup": {
    "auth": { "kind": "apiKey", "value": "$vars.apiKey" },
    "headers": { "X-Tenant-Id": "$vars.tenant" }
  },
  "steps": [
    {
      "kind": "assert_error",
      "operationId": "listJobs",
      "headers": { "X-Conformance-Script": "server-500-three-times" },
      "expect": {
        "errorClass": "RetryExhaustedError",
        "status": 500,
        "fields": {
          "error.code": "internal_error"
        }
      }
    }
  ]
}
