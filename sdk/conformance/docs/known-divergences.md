# Known Divergences

Empty at initial landing — the full conformance matrix is green
(20 fixtures × 3 SDKs = 60 cells, all PASS). When a genuine
cross-SDK difference is accepted (with human sign-off), tag the
fixture with `"tags": ["known-divergence"]` and record it here
with:

* **Scenario** — the failing fixture id.
* **SDK(s)** — which harnesses disagree.
* **Behavior** — what each SDK does.
* **Why accepted** — business or technical rationale.
* **Tracking task** — the Moe task id that will close this out.

No entries yet.
