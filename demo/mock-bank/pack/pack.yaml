apiVersion: cordum.io/v1alpha1
kind: Pack

metadata:
  id: demo-mock-bank
  version: 0.3.0
  title: Mock Bank Demo
  description: Mock bank demo with workflow-routed transfers and approval gates.

compatibility:
  protocolVersion: 1
  minCoreVersion: 0.6.0

topics:
  - name: job.demo-mock-bank.transfer
    capability: demo-mock-bank.transfer
    riskTags:
      - finance
      - transfer

resources:
  workflows:
    - id: demo-mock-bank.transfer
      path: workflows/transfer.yaml

overlays:
  config:
    - name: pools
      scope: system
      key: pools
      strategy: json_merge_patch
      path: overlays/pools.patch.yaml
  policy:
    - name: mock-bank
      strategy: bundle_fragment
      path: overlays/policy.fragment.yaml
