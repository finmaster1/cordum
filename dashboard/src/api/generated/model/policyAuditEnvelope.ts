/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Cordum HTTP API
 * Canonical OpenAPI 3.0.3 spec for the Cordum gateway HTTP surface.
 * OpenAPI spec version: 2026-05-09.2
 */
import type { PolicyAuditEntry } from "./policyAuditEntry";

/**
 * Paginated envelope returned by `GET /api/v1/policy/audit`. The
`type=output` special path on the same endpoint returns only the
`items` field; consumers should treat `total` / `has_more` /
`offset` as optional when filtering by that type.

 */
export interface PolicyAuditEnvelope {
  items: PolicyAuditEntry[];
  /** Total entries matching the filter (pre-pagination). */
  total?: number;
  /** True when more entries are available past the current window. */
  has_more?: boolean;
  /** Echo of the requested offset (default 0). */
  offset?: number;
}
