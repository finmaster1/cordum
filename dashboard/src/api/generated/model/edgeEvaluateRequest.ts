/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Cordum HTTP API
 * Canonical OpenAPI 3.0.3 spec for the Cordum gateway HTTP surface.
 * OpenAPI spec version: 2026-05-09.2
 */
import type { EdgeEvaluateRequestLayer } from "./edgeEvaluateRequestLayer";
import type { EdgeEvaluateRequestInputRedacted } from "./edgeEvaluateRequestInputRedacted";
import type { EdgeEvaluateRequestToolInputRedacted } from "./edgeEvaluateRequestToolInputRedacted";
import type { EdgeLabels } from "./edgeLabels";
import type { EdgeArtifactPointer } from "./edgeArtifactPointer";

/**
 * Redacted Edge action evaluation request for an existing session/execution. Do not send raw `tool_input`, `tool_result`, `raw_input`, `raw_transcript`, or `transcript`; send bounded `input_redacted`/`tool_input_redacted` plus `artifact_ptrs`.
 */
export interface EdgeEvaluateRequest {
  /** Optional caller-supplied event ID; omitted values are generated by the server. */
  event_id?: string;
  /** Optional body tenant; when present it must match X-Tenant-ID. */
  tenant_id?: string;
  principal_id: string;
  session_id: string;
  execution_id: string;
  agent_product?: string;
  layer: EdgeEvaluateRequestLayer;
  /** Non-empty Edge event kind such as `hook.pre_tool_use`. */
  kind: string;
  /** Hook tool name such as `Bash`; required for hook-layer tool evaluations. */
  tool_name?: string;
  tool_use_id?: string;
  /**
   * Bounded, redacted action input. The server re-redacts and computes the persisted `input_hash`.
   * @nullable
   */
  input_redacted?: EdgeEvaluateRequestInputRedacted;
  /**
   * Claude-hook-friendly alias for `input_redacted`.
   * @nullable
   */
  tool_input_redacted?: EdgeEvaluateRequestToolInputRedacted;
  /** Optional client hash. When redacted input is supplied, the server computes/overwrites the persisted `sha256:` hash from accepted redacted input. */
  input_hash?: string;
  /** Claude-hook-friendly alias for `input_hash`. */
  tool_input_hash?: string;
  cwd?: string;
  repo?: string;
  git_remote?: string;
  git_branch?: string;
  git_sha?: string;
  /** Optional client hint; ignored/overridden by the server-side classifier when derivable. */
  action_name?: string;
  /** Optional client hint; ignored/overridden by the server-side classifier when derivable. */
  capability?: string;
  /** Optional client hints. Server-side classifier output is authoritative. */
  risk_tags?: string[];
  labels?: EdgeLabels;
  /**
   * References to redacted external artifacts for large or sensitive evidence.
   * @maxItems 32
   */
  artifact_ptrs?: EdgeArtifactPointer[];
  /**
   * Optional override of the default 5-minute approval TTL when policy
returns REQUIRE_APPROVAL (EDGE-059). Bound to [1, 300] seconds
server-side; values outside this range or omitted use the 5-minute
default. Callers can ONLY shorten the TTL — values >300 are
silently capped at the default. Used by e2e tests that need to
exercise approval expiration inside a bounded sleep window.

   * @minimum 1
   * @maximum 300
   */
  approval_ttl_seconds?: number;
}
