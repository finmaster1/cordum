/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Cordum HTTP API
 * Canonical OpenAPI 3.0.3 spec for the Cordum gateway HTTP surface.
 * OpenAPI spec version: 2026-05-09.2
 */
import type { PolicyShadowMetadata } from "./policyShadowMetadata";

/**
 * Full stored representation of a shadow candidate policy. `content`
is the raw YAML source — consumers that only need to list shadows
may prefer the server-side summary projection (not exposed as a
separate endpoint today).

 */
export interface PolicyShadow {
  /** Stable `shadow-<12 hex>` handle that persists across reactivation. */
  shadow_bundle_id: string;
  /** Active bundle this shadow is tied to (one shadow per bundle). */
  bundle_id: string;
  tenant_id: string;
  /** Raw YAML source of the candidate policy. */
  content: string;
  created_at: string;
  /** Bumped on each re-activation; equals `created_at` on the first. */
  activated_at: string;
  /** Principal ID that activated the shadow. */
  created_by: string;
  /** Arbitrary operator-supplied key/value pairs (e.g. ticket refs). */
  metadata?: PolicyShadowMetadata;
}
