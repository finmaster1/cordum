/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Cordum HTTP API
 * Canonical OpenAPI 3.0.3 spec for the Cordum gateway HTTP surface.
 * OpenAPI spec version: 2026-05-09.2
 */
import type { EdgeApprovalStatus } from "./edgeApprovalStatus";
import type { EdgeApprovalDecision } from "./edgeApprovalDecision";
import type { EdgeLabels } from "./edgeLabels";
import type { EdgeApprovalMetadata } from "./edgeApprovalMetadata";

/**
 * Tenant-scoped action approval bound to an Edge session/execution/event, action hash, and policy snapshot. Raw action/tool payloads are not stored here; use hashes and artifact pointers for evidence.
 */
export interface EdgeApproval {
  /** @pattern ^edge_appr_[A-Za-z0-9_-]+$ */
  approval_ref: string;
  tenant_id: string;
  session_id: string;
  execution_id: string;
  event_id: string;
  /** Principal that requested the governed action. */
  principal_id: string;
  /** Authenticated requester identity used for self-approval protection. */
  requester: string;
  /** Principal that approved/rejected/expired the approval. */
  resolver_id?: string;
  /** Display/audit identity for the resolver. */
  resolved_by?: string;
  status: EdgeApprovalStatus;
  /** Empty while pending; terminal decisions match status. */
  decision?: EdgeApprovalDecision;
  /** Trigger reason explaining why approval was required. */
  reason: string;
  /** Resolver-provided reason or system expiry reason for terminal records. */
  resolution_reason?: string;
  rule_id: string;
  /** Redacted policy snapshot identifier; used to reject stale approvals. */
  policy_snapshot: string;
  /** Stable action hash. The raw action payload is not stored. */
  action_hash: string;
  /** Stable hash of the redacted/bounded action input used for evidence correlation. */
  input_hash: string;
  created_at: string;
  /** @nullable */
  expires_at: string | null;
  /** @nullable */
  resolved_at?: string | null;
  /**
   * Set once an approved approval is atomically claimed by the agent retry path.
   * @nullable
   */
  consumed_at?: string | null;
  labels?: EdgeLabels;
  metadata?: EdgeApprovalMetadata;
}
