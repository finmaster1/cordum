/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Cordum HTTP API
 * Canonical OpenAPI 3.0.3 spec for the Cordum gateway HTTP surface.
 * OpenAPI spec version: 2026-05-09.2
 */
import type { InstalledPackVerificationSignatureAlgorithm } from "./installedPackVerificationSignatureAlgorithm";

/**
 * Pack-signature verification outcome computed by the gateway at
install time. Never client-supplied — the gateway discards any
`verification` field on the install payload and computes its own.
Pre-existing installs default to {signed: false} when this object
is absent.

 */
export interface InstalledPackVerification {
  /** True when the gateway successfully verified the pack against
a trusted publisher key at install time.
 */
  signed: boolean;
  /** Publisher identifier derived from the trusted key's metadata. */
  publisher_id?: string;
  /** Key id of the Ed25519 public key that verified the pack. */
  kid?: string;
  /** RFC3339 timestamp of the server-side verification. */
  verified_at?: string;
  /** True when a valid Cordum counter-signature envelope
(pack.yaml.sig.cordum) is present and verified against the
embedded Cordum counter-signing key.
 */
  has_cordum_counter_sig?: boolean;
  /** Algorithm advertised by the verified envelope. Always ed25519 today. */
  signature_algorithm?: InstalledPackVerificationSignatureAlgorithm;
  /**
   * Canonical manifest version that was signed.
   * @minimum 1
   */
  pack_signature_version?: number;
  /** Non-fatal warnings the gateway wants to surface alongside the
verification outcome (for example, "pack accepted unsigned —
gateway strict mode disabled").
 */
  warnings?: string[];
}
